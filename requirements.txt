flask==3.0.3
waitress==3.0.0
# Pin numpy explicitly before pandas to ensure wheel install and avoid building from source on some platforms
numpy==1.26.4
pandas==2.2.2
openpyxl==3.1.5
psycopg2-binary==2.9.9
python-dotenv==1.0.1
